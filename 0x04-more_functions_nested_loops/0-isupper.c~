#include "main.h"

/**
 * _isupper - it checks for uppercase alphabet
 *
 * @c: the input value of the _isupper function
 *
 * Return: Always 0
 *
 */


int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);}
	else
	{
		return (0);
	}
}
