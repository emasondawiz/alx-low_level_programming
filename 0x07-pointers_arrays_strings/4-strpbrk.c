#include "main.h"

/**
 * *_strpbrk - Function that searches for a pattern
 * @s: Input value
 * @accept: Input value
 * Return: Always 0
 */

char *_strpbrk(char *s, char *accept)
{
	return (strpbrk(s, accept));
}
