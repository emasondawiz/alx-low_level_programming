#include "main.h"

/**
 * *_strstr - function that get the first occurence and prints everything
 * @haystack: Input variable
 * @needle: Input variable
 * Return: Always 0
 *
 */

char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
