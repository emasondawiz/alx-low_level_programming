#include "main.h"

/**
 * _strncat - Combines two string
 *
 * @dest: Input variable
 * @src: Input variable
 * @n: Input variable
 *
 * Return: Always 0
 *
 */

char *_strncat(char *dest, char *src, int n)
{
	return (strncat(dest, src, n));
}
