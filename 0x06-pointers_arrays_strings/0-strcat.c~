#include "main.h"

/**
 * _strcat - Combines two string
 *
 * @dest: Input variable
 * @src: Input variable
 *
 * Return : Always 0
 *
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
